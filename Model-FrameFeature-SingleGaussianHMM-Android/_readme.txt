This codebase creates model based on frame-level features. 

It creates a single Gaussian model for the mixture of all non-speech classes. 

It does not calculate features using Matlab. Instead, it loads features calculated in Android and then trains and tests the model.